Investigating the effect of Inoculation Prompting on cross-trait suppression in LLMs, and its correlation with the cosine similarity of the trait + IP activation vectors in the model's residual stream.